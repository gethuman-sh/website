baseURL = "https://gethuman.sh/"
locale = "en"
title = "human - The AI dev rig for AI coding agents"
disableKinds = ["taxonomy", "term"]

[markup.goldmark.renderer]
  unsafe = true

[permalinks]
  articles = "/:contentbasename/"

[params]
  description = "human is your AI dev rig: every best-of-breed tool your AI development needs, secured and wired around the coding agent you already run. Connect Jira, GitHub, Linear, Notion, Figma, Amplitude and Sentry with 95% fewer tokens, guardrails, audit trail, and context engineering."
  ogDescription = "Your AI dev rig: every best-of-breed tool your AI development needs, secured and wired around the coding agent you already run. 95% fewer tokens, guardrails, and full context."
  githubRepo = "https://github.com/StephanSchmidt/human"
  githubAPI = "https://api.github.com/repos/StephanSchmidt/human"

# Canonical author entity, referenced from JSON-LD (see partials/jsonld-person.html).
# Add more profile URLs to sameAs (X, LinkedIn, Mastodon) to strengthen entity resolution.
[params.author]
  name = "Stephan Schmidt"
  url = "https://www.amazingcto.com/"
  sameAs = [
    "https://www.amazingcto.com/",
    "https://github.com/StephanSchmidt",
    "https://www.linkedin.com/in/stephanjschmidt/",
  ]

[params.organization]
  name = "human"
  sameAs = [
    "https://github.com/StephanSchmidt/human",
  ]

# Plain-text llms.txt output for AI crawlers (rendered by layouts/index.llms.txt).
[outputFormats.llms]
  mediaType = "text/plain"
  baseName = "llms"
  isPlainText = true
  notAlternative = true

[outputs]
  home = ["HTML", "RSS", "llms"]
